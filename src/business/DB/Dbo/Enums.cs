namespace Business.DB.Dbo
{
    public enum Enums
    {
        Unknow = 0,
        Jpg,
        Png,
        Gif,
        Mpg,
        Avi
    }
    public enum EFolderType { Repository, Drop }
    public enum EFolderStatus { Discovering, DiscoverComplete }
}
